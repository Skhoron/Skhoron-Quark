# branch_number.py и avalanche_test.py используют только стандартную
# библиотеку Python — зависимостей не требуют.
#
# Ниже — опциональные зависимости для полноценной реализации
# differential_search.py (не установлены и не используются в текущей
# заготовке, см. docstring в файле):
#
# pycryptosat>=5.11   # SAT solver для поиска differential trail
# pulp>=2.7            # MILP через открытые солверы (CBC) как альтернатива